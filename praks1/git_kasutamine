* root kasutajana
install - sudo apt-get install git
* tavakasutajana:
esmakordselt:

a) seadista - maara kasutajanimi, email, editor
git config --global user.name "Kristo R"
git config --global user.email kristoy0@hotmail.com
git config --global core.editor nano

b) loo kataloog kus hakkad skriptima
sudo mkdir skriptimine

c)sisene antud kausta
cd skriptimine

d)tekita lokaalne repositoorium git systeemi jaoks antud kaustas
git init

remote seadistamine:

a)git remote add nimi address
git remove add skriptimine https://github.com/kristoy0/skriptimine.git

b)git remote kontroll

git remove -v

põhikasutamine:

a) pane fail jalgimisele:
git add failinimi

b)lisa kirjeldus muudatuste kohta
git commit -m "kirjeldus"

c) lisa kaugrepositooriumi
git push -uf skriptimine master
